#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

//#include "risk_model.h"
#include "risk_view_initialize.h"

int main(void){

    country_t* countries;
    uint nb_countries;

    continent_t* continents;
    uint nb_continents;

    user_t* users;
    uint nb_players;

    

    free(countries);
    free(continents);
    free(users);

    return EXIT_SUCCESS;
}
